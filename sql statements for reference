ALTER TABLE `users` ADD `phone` VARCHAR(30) NOT NULL DEFAULT 'You have not Added Your Phone' AFTER `signupDate`;
UPDATE `users` SET `profileChange` = NULL, `lastLogin` = NULL, `phone` = '+254729161266' WHERE `users`.`userId` = 4;
ALTER TABLE `items` ADD `itemCategory` VARCHAR(50) NOT NULL AFTER `price`, ADD `subCategory` VARCHAR(50) NOT NULL AFTER `itemCategory`, ADD `model` VARCHAR(20) NOT NULL AFTER `subCategory`, ADD `itemImage` VARCHAR(200) NOT NULL DEFAULT 'itemimages/default.png' AFTER `model`, ADD `dateAdded` TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP AFTER `itemImage`, ADD `availability` VARCHAR(20) NOT NULL DEFAULT 'available' AFTER `dateAdded`, ADD `boughtTimes` INT(5) NOT NULL DEFAULT '0' AFTER `availability`, ADD `favorite` VARCHAR(10) NOT NULL DEFAULT 'favorite' AFTER `boughtTimes`;
